import { IManifest } from './types';
declare const template: (name: string, description: string, icon: string, manifest: IManifest, installAvailable: any, hideDialog: any, howToForApple: any, howToRequested: boolean) => import("lit-element").TemplateResult;
export default template;
